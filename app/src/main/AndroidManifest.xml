<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isu.erp.eaccount">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PopupActivity"></activity>
        <activity android:name=".activity.B001DetailActivity" />
        <activity android:name=".activity.CalendarActivity" />
        <activity android:name=".activity.B005Activity" />
        <activity android:name=".activity.B004Activity" />
        <activity android:name=".activity.B003Activity" />
        <activity android:name=".activity.B002Activity" />
        <activity android:name=".activity.B001Activity" />
        <activity android:name=".activity.A003Activity" />
        <activity
            android:name=".activity.A002Activity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.A001Activity"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>